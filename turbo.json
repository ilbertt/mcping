{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build:bundle": {
      "dependsOn": ["^build:bundle"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "check:types": {
      "dependsOn": ["^check:types"]
    }
  }
}
